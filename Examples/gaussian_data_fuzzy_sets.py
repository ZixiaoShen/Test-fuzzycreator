# This module provides example of creating polling type-1 fuzzy sets.


# def load_distributions(total_sets=3):
#     """Loads the data that is created by the function load_distributions."""
fsock = open('data/polling_data', "r")
